Using G++ to compile multiple .cpp and .h files

```
g++ main.cpp other.cpp etc.cpp
```

Using G++ to compile multiple .cpp and .h files

```
g++ -c myclass.cpp
g++ -c main.cpp
g++ myclass.o main.o
./a.out
```

